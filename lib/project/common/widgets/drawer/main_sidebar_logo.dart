import 'package:flutter/material.dart';

import '../app_logo.dart';

class MainSidebarLogo extends StatelessWidget {
  const MainSidebarLogo({super.key});

  @override
  Widget build(final BuildContext context) {
    return AppLogo(
      onTap: () {},
      width: 192,
      height: 192,
    );
  }
}
